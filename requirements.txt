# ============================================================
# requirements.txt   Algo Trading Platform
# Python 3.11+
# ============================================================

# --- Streamlit ---
streamlit==1.45.1
streamlit-autorefresh==1.0.1

# --- Data ---
yfinance>=0.2.60
# Pinned exact (not >=): unpinned pandas/numpy let a container rebuild
# silently pick up a newer major version than what's tested locally,
# which is the leading suspect behind a pandas-dtype report bug that
# reproduced in prod but not against the locally installed versions.
pandas==2.2.3
numpy==2.2.5

# --- Technical Analysis ---
ta==0.11.0

# --- Plotting ---
plotly>=6.0.0

# --- Networking / Alerts ---
requests>=2.31.0
python-dotenv>=1.0.0

# --- Scheduler ---
APScheduler>=3.10.4

# --- Database (Azure SQL) ---
pyodbc>=5.1.0

# --- Utils ---
pytz>=2024.1
typing_extensions>=4.14.1
watchdog>=6.0.0
upstox-python-sdk>=2.28.0

# --- Auth (Microsoft Entra ID) ---
msal==1.37.0

# --- Reporting (Excel export) ---
openpyxl==3.1.5
